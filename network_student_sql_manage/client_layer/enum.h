#pragma once

enum emListMain
{
  id = 0,
  name,
  sex,
  phone,
  hobby,
};

enum emComboSex
{
  unkonw = 0,
  man,
  woman,
};

enum emTimer
{
  progress = 0,
};

enum emMessage
{
  connected = 0,
  connect_fail,

};